name: Go1 Build Catkin

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev


jobs:
  build:
    runs-on: ubuntu-latest
    name: Build
    
    steps:
      - name: run
        uses: ./.github/workflows/go1catkin
